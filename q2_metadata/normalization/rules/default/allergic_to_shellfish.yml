# variable: allergic_to_shellfish
# blank: "Not applicable"
# Definition: Is the participant allergic to shellfish? Maps to  in rounds 1-15
# expected: "true", "false"
# Helpful hints: select all
# missing: "Not provided"
blank: Not applicable
expected:
- true
- false
format: bool
missing: Not provided
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
