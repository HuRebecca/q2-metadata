# variable: state
# blank: "Not applicable"
# Definition: The state in which the participant lives
# Helpful hints: nan
blank: Not applicable
format: str
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
