# variable: depth
# blank: "0"
# Definition: 0 or depth underground in meters
# expected: "0"
# Helpful hints: mimarks
blank: 0
expected:
- 0
format: int
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
