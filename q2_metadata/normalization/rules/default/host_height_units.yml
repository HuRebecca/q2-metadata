# variable: host_height_units
# Definition: cm or inches
# Examples:

#	* not applicable: "Not applicable"
#	* not provided: "Not provided"
#	* cm: "cm"
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
