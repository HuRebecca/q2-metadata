# variable: consume_animal_products_abx
# blank: "Not applicable"
# Definition: Does the participant eat animal products treated with antibiotics?
# expected: "true", "false", "Not sure"
# Helpful hints: nan
blank: Not applicable
expected:
- true
- false
- Not sure
format: str
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
