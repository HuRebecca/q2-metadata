# variable: csection
# blank: "Not applicable"
# Definition: Was the participant born via c-section?
# expected: "true", "false", "Not sure"
# Helpful hints: nan
# missing: "Not provided"
blank: Not applicable
expected:
- true
- false
- Not sure
format: bool
missing: Not provided
remap:
  BEFORE: AFTER
normalization:
  gated_value: Out of bounds
  maximum: 120
  minimum: 0
validation:
  force_to_null_if:
    is null:
    - host_subject_id
    - host_taxid
  warn_if_null:
  - country
